<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Head -->
  <title><%= title %></title>
  <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/@fortawesome/fontawesome-free/css/all.min.css">
  <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
  <!-- Main navbar -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <!-- Brand/logo -->
    <a class="navbar-brand" href="#">
      <img src="img/logo.png" alt="logo" style="width:100px;">
    </a>
    
    <!-- Links -->
    <ul class="navbar-nav">
      <li class="nav-item">
            <% if(title == 'Home') { %>
                <a class="nav-link active"
            <%} else { %>
                <a class="nav-link"
            <% } %>
            href="/"><i class="fas fa-home"></i> Home</a>
      </li>
      <li class="nav-item">
        <% if(title == 'Projects') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/projects"><i class="fas fa-tasks"></i> Projects</a>
      </li>
      <li class="nav-item">
        <% if(title == 'About') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/about"><i class="fas fa-address-card"></i> About</a>
      <li class="nav-item">
        <% if(title == 'Users') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/users"><i class="fas fa-user"></i> Users</a>
      </li>
    </ul>
  </nav>