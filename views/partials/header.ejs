<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Head -->
  <title><%= title %></title>
  <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/@fortawesome/fontawesome-free/css/all.min.css">
  <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
    <div id="allPage">
  <!-- Main navbar -->
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <!-- Brand/logo -->
    <a class="navbar-brand" href="#">
      <img src="img/logo.png" alt="logo" style="width:130px; margin-left: 20px;">
    </a>
    
    <!-- Links -->
    <ul class="navbar-nav">
      <li class="nav-item">
            <% if(title == 'Home') { %>
                <a class="nav-link active"
            <%} else { %>
                <a class="nav-link"
            <% } %>
            href="/"><i class="fas fa-home"></i> Home</a>
      </li>
      <li class="nav-item">
        <% if(title == 'Projects') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/projects"><i class="fas fa-tasks"></i> Projects</a>
      </li>
      <li class="nav-item">
        <% if(title == 'Services') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/services"><i class="fas fa-suitcase"></i> Services</a>
      </li>
      <li class="nav-item">
        <% if(title == 'About') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/about"><i class="fas fa-address-card"></i> About Me</a>
      </li>
      <li class="nav-item">
        <% if(title == 'Contact') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/contact"><i class="fas fa-envelope"></i> Contact Me</a>
      </li>
      <li class="nav-item">
        <% if(title == 'Business Contact') { %>
            <a class="nav-link active"
        <%} else { %>
            <a class="nav-link"
        <% } %>
        href="/businessContact"><i class="fas fa-tasks"></i> Business Contact</a>
      </li>
      <li class="nav-item">
        <% if (typeof userName == 'string' && userName) { %>
          <a class="nav-link" href="/users/signout" onclick="return confirm('Are you sure?')">Sign Out (<%= userName %>)</a>
        <%} else { %>
          <a class="nav-link" href="/users/signin"><i class="fas fa-lock"></i>Signin</a>
        <% } %>
      </li>
    </ul>
  </nav>
  <div id="Content">